# Their Markets Comparison

A standalone Next.js app that keeps only the Their Markets product comparison page.

## Development

```bash
npm install
npm run dev
```

Open `/comparison/demo` in development to view the built-in preview comparison when the remote comparison API is unavailable.
